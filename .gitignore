# ---------- .NET ----------
[Bb]in/
[Oo]bj/
*.user
*.suo
.vs/
[Dd]ebug/
[Rr]elease/
[Ll]og/
[Ll]ogs/
*.log
project.lock.json
artifacts/

# ---------- Node / Angular ----------
node_modules/
dist/
.angular/
npm-debug.log*
yarn-error.log*
*.tsbuildinfo
.karma/
coverage/

# ---------- Env / secrets ----------
# Keep appsettings.json (dev connection string only). Ignore per-machine overrides:
appsettings.*.local.json
*.local.json
.env
.env.*

# ---------- Local tooling ----------
# Claude Code's per-machine state: settings, skills, and worktrees holding build
# output. Never belongs in a commit — it stayed untracked by hand until now, and a
# stray `git add -A` had already come close to sweeping it in.
.claude/

# ---------- OS / editor ----------
Thumbs.db
ehthumbs.db
Desktop.ini
.DS_Store
*.swp
.gstack/
